<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectand">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectAnd">
        <activity android:name=".TimeActivity"></activity>
        <activity android:name=".AddQuesActivity" />
        <activity android:name=".EditQuesActivity" />
        <activity android:name=".ListQuesActivity" />
        <activity android:name=".AddSubActivity" />
        <activity android:name=".KeyActivity" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".SubjectActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ResultActivity" />
    </application>

</manifest>